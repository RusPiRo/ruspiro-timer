# The listed toolchain setup is reflecting the Rust version
# the crate can be safely build with. The target mentioned is the
# one required to build the crate in the pipeline
# local builds will use the "aarch64-ruspiro.json" target file
[toolchain]
channel = "nightly-2021-04-24"
components = [ "rust-src", "llvm-tools-preview" ]
profile = "minimal"
target = "aarch64-unknown-none"